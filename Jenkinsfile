pipeline{
    agent any
    stages{
        stage("A"){
            steps{
                echo "========executing A========"
                sh "hostname"
                sh "free -m"
                sh "hostname"
                sh "welcome"
            }
            
        }
    }
}